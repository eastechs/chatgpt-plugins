// Placeholder. Run `node scripts/sync-model-pricing.js` to populate this file
// with a real snapshot from LiteLLM. The file ships with the app as the
// fallback when the runtime refresh fails or has yet to complete.

import type { RawPricingData } from "../pricing.js";

export const BUNDLED_PRICING: RawPricingData = {
  source: "placeholder",
  fetched_at: "1970-01-01T00:00:00.000Z",
  models: {},
};
